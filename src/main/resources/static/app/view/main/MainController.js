Ext.define('App.view.main.MainController', {
    extend: 'Ext.app.ViewController',

    alias: 'controller.main',

    onGridSelected: function (sender, record) {
        if (record) {
            //this.getForm().loadRecord(record);
        }
    	
    	
    }

});
